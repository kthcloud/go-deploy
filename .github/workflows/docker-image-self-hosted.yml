name: Generate artifacts

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: self-hosted
    steps:
      - name: my-step
        run: echo "Hello World!"
