name: Build types

on:
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Build types
      run: |       
        cd scripts
        chmod +x ./generate-types.sh
        ./generate-types.sh
        cd ..
      